import undisposed from './undisposed'
import computed from './computed'
import observable from './observable'
import action from './action'
import reaction from './reaction'
import refra from './refra'
import on from './on'
import eventable from './eventable'
import connection from './connection'

const prop = observable // 兼容老版本
const obx = observable // 简称

const child = connection // 别名
const connx = connection // 别名

export {
  undisposed,
  observable,
  prop,
  obx,
  computed,
  action,
  reaction,
  refra,
  on,
  eventable,
  connection,
  child,
  connx
}

export default {
  undisposed,
  observable,
  prop,
  obx,
  computed,
  action,
  reaction,
  refra,
  on,
  eventable,
  connection,
  child,
  connx
}
